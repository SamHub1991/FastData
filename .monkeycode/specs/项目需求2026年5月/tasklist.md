# 项目需求2026年5月 - 实施任务清单

## 1. 架构优化

- [ ] 梳理现有多数据库重复代码。
- [ ] 抽取数据库适配器接口。
- [ ] 抽取 SQL 方言接口。
- [ ] 抽取元数据读取接口。
- [ ] 将工具项目与 ORM 核心项目分离。

## 2. 多数据库配置简化

- [ ] 新增统一 `Connections` 配置结构。
- [ ] 保留旧配置格式兼容读取。
- [ ] 实现默认数据库配置。
- [ ] 实现 `FastRead.Use(key)` 和 `FastWrite.Use(key)`。
- [ ] 实现 `FastDb.Use(key)` 作用域切换。
- [ ] 实现 `IFastRepositoryFactory` 指定库 Repository。
- [ ] 补充配置错误提示和可用 Key 提示。

## 3. Model 生成工具

- [ ] 新建 `FastData.Tooling` 公共工具项目。
- [ ] 新建 `FastData.ModelGenerator.WinForms` 项目。
- [ ] 实现数据库连接测试。
- [ ] 实现表加载、搜索、多选和字段预览。
- [ ] 实现默认命名空间和单表命名空间配置。
- [ ] 实现 Model 代码预览、编辑和生成。
- [ ] 验证生成 Model 可编译。

## 4. 数据同步工具

- [ ] 新建 `FastData.SyncTool.WinForms` 项目。
- [ ] 实现源库、中间库、目标库配置。
- [ ] 设计中间库表结构。
- [ ] 实现 SQL Server、MySQL、Oracle 中间库脚本生成。
- [ ] 实现中间库自动创建和 SQL 导出。
- [ ] 实现全量同步。
- [ ] 实现增量同步。
- [ ] 实现同步重试、错误记录和任务恢复。
- [ ] 实现中间库历史数据清理。
- [ ] 实现任务状态和错误日志界面。

## 5. 中文文档

- [ ] 编写快速开始文档。
- [ ] 编写多数据库配置和优雅切换文档。
- [ ] 编写 Model 生成工具文档。
- [ ] 编写数据同步工具文档。
- [ ] 编写 XML SQL Map、Repository、AOP 和 FAQ 文档。

## 6. 验证

- [ ] 验证原有 ORM API 兼容。
- [ ] 验证默认库和指定库切换写法。
- [ ] 验证 Model 生成工具完整流程。
- [ ] 验证中间库 SQL 自动创建和导出。
- [ ] 验证源库到目标库端到端同步。
- [ ] 验证失败重试、任务恢复和中间库清理。
